const fs = require('fs');
// readFile(path[, options])
// createReadStream(path[, options])
// writeFile(file, data[, options])
// createWriteStream(path[, options])
//createReadStream and createWriteStream are recommended because they are stream
//readFile and writeFile are buffer, occupying much more memories

// appendFile(path, data[, options])
// copyFile(src, dest[, flags])
// stat(path[, options])
// access(path[, mode]), chmod(path, mode), chown(path, uid, gid)
// link(existingPath, newPath), unlink(path)
// truncate(path[, len])

// mkdir(path[, mode])
// readdir(path[, options])
// rmdir(path)
// rename(oldPath, newPath)
