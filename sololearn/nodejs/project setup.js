/**
 * npm install 'package_name' --save
 * this will create both the node_modules directory and save it inside the package.json for further use.
 * use some command inside the package.json to run our server
 * You can run any JavaScript code using the node by typing 'node' in the terminal
 * 
 */