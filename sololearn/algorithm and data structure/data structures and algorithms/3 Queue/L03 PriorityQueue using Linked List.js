function PriorityQueue() {
    var list = new LinkedList();
    
    this.enQueue = function(element) {
        //首先，判断是不是队列为空，如果为空，则直接插入


        //否则，就和第一个进行比较，依次循环，直到找到比它小的。

        //如果是最后一个，直接addLast，如果是在中间的，则用addAfter
    };

    this.deQueue = function() {
        //
    };

    
}