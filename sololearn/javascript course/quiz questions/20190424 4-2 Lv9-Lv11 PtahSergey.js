/**
 * Q4
 * reverse()不需要接收参数。
 */
const newArr = ["str","false","true"];
function join() {
    for (let i = 2, arr=newArr.length; i <=2; ) {
        console.log(newArr.reverse(arr)[i]);
        i++;
    }
}
join();//str